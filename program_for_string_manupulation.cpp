#include<stdio.h>
#include<conio.h>
#include<string.h>
 
int main()
{     
   int i;
        char a[10]="darshika";
        for(i=0;a[i]!='\0';i++)
        {
        	printf("letter=%c\n ",a[i]);
        	printf("ascii value=%d\n",a[i]);
        	
		}
	   
	   return 0;
}
